[pytest]
# pytest 配置
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 异步测试模式
asyncio_mode = auto

# 输出详细信息
addopts = -v --tb=short

# 标记
markers =
    slow: 标记慢速测试
    integration: 集成测试（需要数据库）
    unit: 单元测试
