# 审查报告 - 剩余工作清单

**生成时间**：2026-06-09  
**对比基准**：v审查报告（21个问题）

---

## 完成情况统计

| 优先级 | 总数 | 已完成 | 未完成 | 完成率 |
|--------|------|--------|--------|--------|
| P0 止血 | 5 | 4 | 1 | 80% |
| P1 可部署 | 5 | 3 | 2 | 60% |
| P2 业务安全 | 5 | 3 | 2 | 60% |
| 低危/质量 | 3 | 1 | 2 | 33% |
| **总计** | **18** | **11** | **7** | **61%** |

---

## P0：止血（4/5 完成）

### ✅ 已完成

| # | 问题 | 修复方式 |
|---|------|----------|
| 1 | 脚本硬编码密码 | 创建 `db_config.py`，15+ 脚本改为读取环境变量 |
| 2 | JWT 默认密钥 | `config.py` 生产环境强制要求 SECRET_KEY |
| 3 | Docker 构建失败 | 移除 apt-get curl，改用 Python 健康检查 |
| 4 | requirements.txt 不完整 | 添加 aiofiles、email-validator |

### 🔴 未完成

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | **轮换已暴露的密钥** | ❌ 需手动 | 数据库密码、API Key 已泄露，必须在平台后台轮换 |

**操作指南**：
```bash
# 1. 生成新 SECRET_KEY
python -c "import secrets; print(secrets.token_urlsafe(64))"

# 2. 更新 .env
SECRET_KEY=新生成的密钥
DB_PASSWORD=新数据库密码
DEEPSEEK_API_KEY=新key
```

---

## P1：可部署可回滚（3/5 完成）

### ✅ 已完成

| # | 问题 | 修复方式 |
|---|------|----------|
| 1 | .dockerignore 排除商品图 | 添加 `!static/products/*.jpg` 等例外 |
| 2 | CI/CD PR 触发部署 | 添加 `if: github.event_name != 'pull_request'` |
| 3 | 回滚逻辑不可用 | 修复文件路径，添加 validate job |

### 🟡 未完成

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | **部署改为镜像推送模式** | ❌ 未做 | 当前是服务器现场构建，应改为 构建→推送→拉取 |
| 2 | **加 /version 端点** | ❌ 未做 | 显示 commit SHA 和镜像版本 |

**建议实现**：
```python
# api_server.py 添加
@app.get("/version")
async def version():
    return {
        "commit": os.getenv("GIT_COMMIT", "unknown"),
        "build_time": os.getenv("BUILD_TIME", "unknown"),
    }
```

---

## P2：业务安全和测试（3/5 完成）

### ✅ 已完成

| # | 问题 | 修复方式 |
|---|------|----------|
| 1 | 商家接口输入约束 | ProductCreate/CouponCreate 添加 Field 约束 |
| 2 | WebSocket 鉴权 | 改用一次性 ticket 机制 |
| 3 | 前端 XSS 部分修复 | 图片 URL 白名单，通知用 textContent |

### 🟡 未完成

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | **订单状态枚举统一** | ⚠️ 部分完成 | 数据库有 CHECK 约束，但 `returned`/`refunded` 语义未拆分 |
| 2 | **pytest 集成测试** | ❌ 未做 | 仍是脚本式测试，需迁移到 pytest |

**建议**：
- 订单状态：拆分 `order_status` 和 `refund_status`
- 测试：创建 `tests/` 目录，使用 pytest + fixtures

---

## 低危/工程质量（1/3 完成）

### ✅ 已完成

| # | 问题 | 修复方式 |
|---|------|----------|
| 1 | Git 跟踪 node_modules | .gitignore 添加 node_modules/，已从跟踪中删除 |

### 🟡 未完成

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | **项目目录清理** | ❌ 未做 | 大量 tar/zip/截图在根目录 |
| 2 | **Docker Compose 完整化** | ❌ 未做 | 缺少 MySQL/Redis 服务定义 |

**建议**：
```yaml
# docker-compose.dev.yml
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: dev_password
      MYSQL_DATABASE: product_db
    ports:
      - "3306:3306"
  
  redis:
    image: redis:7
    ports:
      - "6379:6379"
```

---

## 未覆盖的审查问题

以下问题在审查报告中提到，但 Codex 未处理：

| # | 问题 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | 运行镜像与源码漂移 | P1 | 需要重新部署验证 |
| 2 | API 文档公开暴露 | P2 | 已修复（生产环境关闭 docs） |
| 3 | 慢查询日志脱敏 | P2 | ✅ 已修复 |
| 4 | 健康检查信息泄露 | P2 | ✅ 已修复 |

---

## 下一步行动

### 立即执行（今天）

1. **轮换密钥**
   - [ ] MySQL root 密码
   - [ ] DeepSeek API Key
   - [ ] OpenAI API Key（如使用）
   - [ ] SiliconFlow API Key
   - [ ] JWT SECRET_KEY

2. **更新 .env**
   - [ ] 填入新密钥
   - [ ] 设置 `ENV=production`

3. **重新部署**
   - [ ] `docker compose down`
   - [ ] `docker compose up -d --build`

### 短期（本周）

4. **添加 /version 端点**
5. **清理项目目录**（归档文件移到 artifacts/）
6. **验证部署**（确保新镜像正常运行）

### 中期（下周）

7. **Docker Compose 完整化**（添加 MySQL/Redis）
8. **pytest 测试迁移**
9. **部署流程改为镜像推送模式**

---

## 验收检查清单

部署后验证：

- [ ] `docker compose build --no-cache` 成功
- [ ] `/health` 返回 200
- [ ] `/api/products` 返回数据
- [ ] `/static/products/*.jpg` 图片可访问
- [ ] `/metrics` 返回 200（需登录）
- [ ] `/docs` 在生产环境不可访问
- [ ] 登录/注册/下单流程正常

---

**报告生成时间**：2026-06-09 22:45
