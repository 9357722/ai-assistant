# 四棒搜索引擎实现总结

## 一、架构概览

```
用户搜索词: "三千左右的苹果手机"
        │
        ▼
┌─────────────────────────────────────┐
│  第1棒：意图解析与查询改写           │
│  - 规则引擎: 中文数字解析、分类识别   │
│  - LLM增强: DeepSeek纠错+语义理解    │
│  输出: {category:"手机", brand:"苹果",│
│         price_min:2100, price_max:3900}│
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  第2棒：多路召回                     │
│  - 文本召回: name LIKE '%手机%'      │
│  - 分类召回: categories表JOIN        │
│  - 品牌召回: name LIKE '%苹果%'      │
│  输出: 去重后的候选商品集合           │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  第3棒：精排打分                     │
│  - 价格匹配度(权重最高)              │
│  - 销量热度                          │
│  - 评分                              │
│  - 库存状态                          │
│  - 品牌过滤(含别名映射)              │
│  输出: 按分数排序的商品列表           │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  第4棒：重排调整                     │
│  - 多样性打散(同平台限制)            │
│  - 分页截取                          │
│  - 清理内部字段                      │
│  输出: 最终搜索结果                   │
└─────────────────────────────────────┘
```

## 二、核心实现

### 2.1 第1棒：意图解析

**文件**: `services/search_engine.py` → `parse_intent()` 方法

#### 规则引擎（快速路径）

```python
def _rule_based_parse(self, query: str) -> Dict[str, Any]:
```

三个维度的规则提取：

| 维度 | 实现方式 | 示例 |
|------|---------|------|
| **价格提取** | 正则 + 中文数字解析器 | "三千左右" → (2100, 3900) |
| **分类识别** | 同义词字典匹配 | "护肤品" → "美妆" |
| **品牌识别** | 关键词列表匹配 | "苹果手机" → brand="苹果" |

**中文数字解析器** (`parse_cn_number` 函数)：
```
"三千" → 3000
"五千" → 5000
"一万" → 10000
```

通过逐字符遍历，利用 `cn_num_map` 字典将中文数字转换为阿拉伯数字。
处理了"千"、"百"、"十"等进位关系。

#### LLM增强（慢速路径）

```python
async def _llm_parse(self, query: str) -> Optional[Dict[str, Any]]:
```

调用 DeepSeek API，用结构化 prompt 让 LLM 返回 JSON 格式的意图解析结果：
- 纠错："苹菓手机" → "苹果手机"
- 意图识别：判断是搜索、对比还是推荐
- 语义扩展：自动关联同义词

**兜底策略**：LLM 失败时自动降级到规则引擎。

### 2.2 第2棒：多路召回

**文件**: `services/search_engine.py` → `multi_recall()` 方法

三路并发召回，用 `dict` 按 product_id 去重：

| 召回路径 | 匹配方式 | 加分 |
|---------|---------|------|
| 文本召回 | `name LIKE '%keyword%' OR description LIKE '%keyword%'` | +3 |
| 分类召回 | `JOIN categories WHERE c.name = '手机'` | +2 |
| 品牌召回 | `name LIKE '%品牌%'` | +2 |

**同义词扩展**：分类召回时，会用 `CATEGORY_SYNONYMS` 字典扩展搜索。
例如"手机"分类会同时搜索"手机"、"智能手机"、"移动电话"等。

### 2.3 第3棒：精排打分

**文件**: `services/search_engine.py` → `score_products()` 方法

打分公式：`总分 = 文本匹配分 + 价格匹配分 + 销量分 + 评分分 - 库存惩罚`

| 维度 | 分值范围 | 说明 |
|------|---------|------|
| 文本/分类/品牌匹配 | +2~+7 | 召回时累加 |
| 价格区间内 | +10 | 权重最高 |
| 价格区间外 | -5~+5 | 按偏离程度线性衰减 |
| 销量热度 | 0~3 | >100:3, >50:2, >10:1 |
| 商品评分 | 0~2 | rating/2.5 |
| 缺货惩罚 | -10 | stock<=0 大幅降权 |

**品牌过滤**（精排后执行）：
```python
# 有品牌匹配 → 直接过滤掉不匹配的
brand_aliases = BRAND_NAME_MAP.get("苹果", ["iphone", "apple", ...])
brand_matched = [p for p in scored if any(alias in name for alias in brand_aliases)]
```

### 2.4 第4棒：重排调整

**文件**: `services/search_engine.py` → `rerank()` 方法

- **多样性打散**：同平台最多展示 4 个，避免全是京东或全是淘宝
- **分页截取**：按页码和每页数量截取
- **字段清理**：移除 `_recall_source`、`_match_score`、`_final_score` 等内部字段

## 三、数据结构

### 意图解析输出
```json
{
    "original_query": "三千左右的苹果手机",
    "corrected_query": "三千左右的苹果手机",
    "keywords": ["苹果", "手机"],
    "category": "手机",
    "brand": "苹果",
    "price_min": 2100,
    "price_max": 3900,
    "attributes": ["三千左右"],
    "intent": "search"
}
```

### 搜索结果输出
```json
{
    "intent": {
        "original_query": "三千左右的苹果手机",
        "corrected_query": "三千左右的苹果手机",
        "category": "手机",
        "brand": "苹果",
        "price_range": {"min": 2100, "max": 3900}
    },
    "total": 4,
    "page": 1,
    "page_size": 20,
    "items": [...]
}
```

## 四、Bug 修复记录

### Bug 1：商品名不含分类关键词导致搜索为空

**问题**：用户搜"手机"，SQL 执行 `name LIKE '%手机%'`，但商品名叫 "iPhone 17 Pro"、"华为Mate 70 Pro"，不包含"手机"两个字。

**根因**：`_query_products` 只搜索商品名，不搜索分类名。

**修复**：SQL 改为同时匹配商品名和分类名：
```sql
WHERE (p.name LIKE %s OR c.name LIKE %s)
```

### Bug 2：中文数字价格提取失败

**问题**：用户搜"三千左右的手机"，价格区间提取失败（返回 None）。

**根因**：正则表达式只支持阿拉伯数字 `(\d+)`，不支持中文数字"三千"。

**修复**：实现 `parse_cn_number()` 中文数字解析器，支持"一二三四五六七八九十千万"的组合解析。正则改为同时匹配中文和阿拉伯数字。

### Bug 3：品牌过滤不生效

**问题**：用户搜"苹果手机"，返回了索尼A7M4、佳能R6等非苹果商品。

**根因**：品牌过滤用 `brand in product_name` 匹配，但"苹果"不在 "iPhone 17 Pro" 名字中。

**修复**：实现品牌别名映射 `BRAND_NAME_MAP`：
```python
"苹果": ["iphone", "apple", "airpods", "macbook", "ipad"]
```
品牌过滤改为匹配所有别名，有匹配时直接过滤掉不匹配的商品。

### Bug 4：价格区间内商品排序靠后

**问题**：搜"三千左右的手机"，¥16999 的索尼排在 ¥2499 的红米前面。

**根因**：价格匹配加分（+5）太低，被分类召回基础分（+2）和销量分（+3）抵消。

**修复**：价格区间内加分从 +5 提高到 +10（权重最高），区间外按偏离程度线性衰减。

### Bug 5：中文数字"左右"的区间计算

**问题**："三千左右"提取为 3000，但区间计算 `3000*0.7=2100, 3000*1.3=3900`，浮动 30% 偏大。

**分析**：30% 浮动是合理的，"三千左右"通常指 2500~3500 范围。当前 2100~3900 稍宽但可接受。

**未修复**：保持 30% 浮动，因为要覆盖用户习惯。

## 五、关键技术决策

| 决策 | 选择 | 原因 |
|------|------|------|
| LLM 用哪个 | DeepSeek-V4-Flash | 项目已接入，速度快成本低 |
| 规则优先还是 LLM 优先 | 规则优先 + LLM 增强 | 规则快（<1ms），LLM 慢（~500ms），LLM 失败时降级到规则 |
| 品牌匹配方式 | 别名映射表 | 比模糊匹配精确，避免误匹配 |
| 价格打分权重 | 最高（+10） | 用户搜带价格时，价格是最强意图信号 |
| 去重方式 | dict 按 product_id | 多路召回可能返回同一商品，需去重 |
| 多样性策略 | 同平台限制 4 个 | 避免结果全是同一平台，提升用户体验 |

## 六、API 接口

```
GET /api/products/search/ai?keyword={搜索词}&page={页码}&page_size={每页数量}
```

**参数**:
- `keyword`: 搜索关键词（必填）
- `page`: 页码，默认 1
- `page_size`: 每页数量，默认 20，最大 100

**返回**:
```json
{
    "intent": { "original_query", "corrected_query", "category", "brand", "price_range" },
    "total": 42,
    "page": 1,
    "page_size": 20,
    "items": [{ "id", "name", "price", "platform", "main_image", "stock", "sales", ... }]
}
```

## 七、后续优化方向

1. **向量召回**：接入 ChromaDB，用 embedding 做语义相似度搜索（当前项目已有 ChromaDB 但未接入搜索引擎）
2. **用户画像**：记录用户点击/购买行为，个性化打分
3. **搜索建议**：输入时实时联想（Typeahead）
4. **搜索纠错展示**：前端展示"已纠正为 XXX"
5. **深度模型**：商品量过万后，用 DeepFM/DIN 替代规则打分
