# 项目全面审核报告（对标京东/淘宝/拼多多）

> 审核日期：2026-06-03
> 审核范围：后端架构、前端UX、部署安全、业务功能
> 对标平台：京东、淘宝、拼多多

---

## 一、综合评分

| 维度 | 当前得分 | 行业标准 | 差距 |
|------|---------|---------|------|
| API 设计 | 5/10 | 9/10 | 缺统一响应格式、版本管理、全局异常处理 |
| 安全防护 | 3/10 | 9/10 | 密钥泄露、无HTTPS、XSS未完全修复 |
| 数据库 | 5/10 | 9/10 | N+1查询、缺索引、事务管理不一致 |
| 前端UX | 4/10 | 9/10 | 缺SKU、物流、售后、搜索建议、移动端适配 |
| 缓存/性能 | 1/10 | 9/10 | 完全没有缓存层 |
| 监控/可观测 | 0.5/10 | 8/10 | 几乎空白 |
| CI/CD | 1/10 | 8/10 | 配置文件损坏 |
| 合规/隐私 | 2/10 | 8/10 | 缺数据删除、审计日志、隐私协议 |

**整体评价**：项目已具备电商核心骨架（用户、商品、购物车、订单、AI），但距离生产上线还有较大差距。

---

## 二、致命问题（必须立即修复）

### 2.1 密钥泄露

| 文件 | 问题 |
|------|------|
| `.env` | 真实 API Key 和数据库密码明文存储 |
| `.mcp.json` | GitHub PAT 泄露 |
| `utils.py` | 两处硬编码 API Key |
| `test_memory.py` | 硬编码 API Key 已入库 |

**行动**：轮换所有泄露密钥，`git filter-repo` 清除历史。

### 2.2 无 HTTPS

所有通信（JWT Token、密码、API Key）明文传输。生产必须上 SSL。

### 2.3 SECRET_KEY 弱密钥

固定开发 SECRET_KEY 可预测，JWT 可被伪造。

### 2.4 依赖未锁定版本

`requirements.txt` 全部依赖无版本号，构建不可复现。

---

## 三、P0 后端架构问题

### 3.1 N+1 查询（性能杀手）

```python
# order.py: 当前代码
for order in orders:
    await cur.execute("SELECT * FROM order_items WHERE order_id = %s", (order["id"],))

# 正确做法：一次批量查询
order_ids = [o["id"] for o in orders]
await cur.execute("SELECT * FROM order_items WHERE order_id IN (%s)", (order_ids,))
# 再在 Python 中按 order_id 分组
```

涉及：`routes/order.py:230`、`routes/admin.py:332`

### 3.2 同步阻塞事件循环

```python
# api_server.py: async 函数中用同步 open()
async def root():
    with open("index.html", "r") as f:  # 阻塞！
        return f.read()
```

涉及：`api_server.py:156-180`（6处）、`utils.py:146`（ChromaDB同步调用）

### 3.3 缺少全局异常处理

未捕获异常返回 HTML 500 页面，API 客户端无法解析。

### 3.4 响应格式不统一

有的返回 `{"message": "成功"}`，有的返回 `{"data": {...}}`，有的返回 `{"error": "..."}`。前端需要为每种格式写不同的解析逻辑。

### 3.5 无缓存层

config.py 定义了 Redis 配置但从未使用。每次请求穿透数据库，AI 调用无缓存。

---

## 四、P0 前端UX问题

### 4.1 核心流程缺失

| 缺失项 | 影响 |
|--------|------|
| 无独立结算确认页 | 结算只有一个弹窗，无支付方式/优惠券/发票 |
| 无SKU规格选择 | 无法选颜色/版本/尺码 |
| 无物流跟踪 | 订单详情无物流时间线 |
| 无售后入口 | 无退换货/退款功能 |
| 移动端搜索栏隐藏 | `display: none` 导致移动端无法搜索 |

### 4.2 搜索体验差距

| 功能 | 京东 | 当前项目 |
|------|------|---------|
| 搜索联想 | 输入即出候选词 | 无 |
| 搜索历史 | localStorage 记录 | 无 |
| 热搜排行 | 搜索框内热词轮播 | 无 |
| AI意图展示 | "您是否要找..." | 仅 console.log |
| 搜索无结果推荐 | "为您推荐" | 无 |

### 4.3 交互细节缺失

- 无骨架屏（加载时白屏→突然出现内容）
- 无加入购物车飞入动画
- `window.confirm()` 原生弹窗（与UI风格不一致）
- 按钮无防重复提交
- 全局 CSS 变量不统一（index.html 用 `Inter` 字体，其他页面用系统字体）

---

## 五、P1 安全与合规问题

### 5.1 安全防护

| 问题 | 严重性 | 说明 |
|------|--------|------|
| SQL 拼接 | 高 | `product_service.py:94` sort_order 未白名单 |
| Token 有效期 12h | 中 | 无 refresh token，无撤销机制 |
| 密码策略宽松 | 中 | 仅 min_length=6，无复杂度要求 |
| 订单号可预测 | 中 | 基于 time.time()，可枚举 |
| 无请求体大小限制 | 中 | 可发送超大 payload 导致 OOM |

### 5.2 合规缺失

| 问题 | 法规要求 |
|------|---------|
| 无用户数据删除接口 | 《个人信息保护法》第47条 |
| 无隐私政策/用户协议 | 《电子商务法》第17条 |
| 无审计日志 | 管理员操作无记录 |
| 地址/手机号明文存储 | 应加密或脱敏 |
| 日志无脱敏机制 | 可能记录用户行为 |

---

## 六、P2 运维与部署问题

### 6.1 监控空白

无 Prometheus 指标、无 APM、无告警、无结构化日志。生产出问题无法排查。

### 6.2 CI/CD 损坏

`.github/workflows/deploy.yml` 内容是 docker-compose.yml，不是 GitHub Actions 配置。

### 6.3 Docker 加固不足

- 镜像未锁定 SHA256 摘要
- 未设置资源限制（mem_limit、cpus）
- 未设置安全选项（no-new-privileges）
- 无只读文件系统

---

## 七、改进路线图

### 第一阶段：安全加固（阻断上线）
1. 轮换所有泄露密钥
2. 启用 HTTPS（Nginx 反代 + Let's Encrypt）
3. 锁定依赖版本
4. 修复 SQL 拼接风险
5. SECRET_KEY 改为高强度随机值

### 第二阶段：核心体验补全
6. 独立结算确认页（支付方式、优惠券、发票）
7. 商品SKU规格选择
8. 搜索联想 + 搜索历史 + AI意图展示
9. 移动端底部Tab栏 + 搜索栏修复
10. 订单物流跟踪

### 第三阶段：架构优化
11. 全局异常处理 + 统一响应格式
12. Redis 缓存层
13. 请求日志中间件
14. N+1 查询修复 + 数据库索引
15. 异步文件 I/O

### 第四阶段：生产运维
16. 结构化日志 + Prometheus 指标
17. CI/CD 流水线修复
18. 数据库备份策略
19. 容器加固
20. 用户数据删除接口 + 审计日志

---

## 八、当前项目优势

尽管差距明显，项目已有以下良好基础：

1. **分层架构** — routes/models/services 分离
2. **搜索引擎** — 四棒架构（意图解析→多路召回→精排→重排）
3. **AI 集成** — DeepSeek LLM + ChromaDB 向量库
4. **JWT 认证** — 角色权限分离（user/admin）
5. **事务管理** — 订单创建使用乐观锁 + 事务
6. **异步编程** — 全面使用 async/await + aiomysql
7. **Docker 化** — 已有 Dockerfile + docker-compose
8. **前端设计** — Apple 风格 UI，悬浮 AI 聊天球
