# AI 智能电商导购平台 — 项目审查报告

> 审查日期：2026-06-02
> 审查范围：全栈（后端 / 前端 / 数据库 / 安全 / 部署 / AI 能力）

---

## 一、项目概况

| 项 | 值 |
|------|------|
| 定位 | AI 驱动的电商导购平台（比价 + 推荐 + 客服） |
| 后端 | FastAPI + LangChain + LangGraph + DeepSeek-V4-Flash |
| 前端 | 原生 HTML/CSS/JS，6 个页面 |
| 数据 | MySQL 8.0 + ChromaDB 向量库 |
| 部署 | Docker Compose + 阿里云 ECS + GitHub Actions |

---

## 二、当前优点（面试能打的点）

### 2.1 架构设计规范

路由层拆分为 6 个独立模块（user / product / cart / order / ai / admin），职责清晰；引入 service 层（ProductService / RecommendationEngine / AICustomerService）做业务隔离，不是把所有逻辑堆在路由里。这在应届生项目里属于加分项。

### 2.2 AI 能力落地完整

RAG（ChromaDB 向量检索）+ Agent（LangGraph 工具调用 + MemorySaver 多轮记忆）+ 流式输出（SSE/ReadableStream），三者串联起来是一个完整的 Agentic RAG 链路。面试时能讲清楚"为什么用 Agent 而不是纯 RAG"——RAG 只能检索，Agent 能推理+行动。

### 2.3 电商核心闭环

用户注册登录（JWT）→ 商品浏览（筛选/排序/分页）→ 购物车 → 下单（事务+乐观锁库存扣减）→ 模拟支付 → 订单管理，链路完整。管理后台还有数据统计、商品/订单/用户/分类 CRUD。

### 2.4 安全意识

改造后 eval() 漏洞已修复，JWT 鉴权覆盖所有敏感端点，API Key 从环境变量读取，订单流程加了事务保证数据一致性，库存扣减用了乐观锁防并发超卖。

### 2.5 工程化程度

Docker Compose 一键编排、GitHub Actions CI/CD 自动部署、环境变量集中管理（config.py）、异步全链路（aiomysql + async/await），具备生产级项目的基本形态。

---

## 三、当前问题（按严重程度排序）

### 3.1 仍需修复（影响正确性/安全性）

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 1 | `web_search` 工具名实不符：查的是本地 `market_news` 表，不是联网搜索 | agent_tools.py | 对用户有误导风险，面试被追问会露馅 |
| 2 | 搜索拆词用双字滑窗：`"华为Mate"` → `"华为"、"为M"、"Ma"、"at"、"te"`，噪音大 | agent_tools.py:46 | 搜索结果不准，产生大量无关命中 |
| 3 | `CORS allow_origins=["*"]` 配合 `allow_credentials=True`，浏览器会拒绝 | api_server.py:35 | 跨域携带 Cookie 场景会失败 |
| 4 | 每个请求创建 DB pool 的问题虽已修复，但 `get_db()` 不再是 generator（`yield`），FastAPI 的 `Depends` 行为可能变化 | routes/*.py | 需要本地运行验证 |
| 5 | `init.sql` 里 categories 表在 products 之后，但 products 引用了 category_id 外键——需要用 `ALTER TABLE`，已在末尾加，但重建数据库时顺序要注意 | init.sql | 初次建库可能报外键错误 |

### 3.2 架构层面待优化

| # | 问题 | 建议 |
|---|------|------|
| 1 | 没有测试：0 个单元测试、0 个集成测试 | 至少给核心链路写 pytest（注册登录、下单、Agent 调用） |
| 2 | 没有日志系统：print() 散落各处 | 引入 loguru 或 structlog，统一日志格式和级别 |
| 3 | 没有错误处理中间件：异常直接抛给客户端 | 加全局 ExceptionHandler，生产环境隐藏堆栈 |
| 4 | `auth.py` 的 SECRET_KEY 有空字符串兜底，意味着不配置也能启动，但签发的 Token 任何人都能伪造 | 启动时检查 SECRET_KEY 非空，否则拒绝启动 |
| 5 | Redis 配置写了但从未使用 | 对话记忆 / 限流 / Session 可用 Redis 持久化 |
| 6 | `utils.py` 和 `recommend.py` 在 api_server.py 清理后仍存在，确认是否还有其他地方依赖 | 确认后可删除或保留为独立工具模块 |

### 3.3 前端待优化

| # | 问题 | 建议 |
|---|------|------|
| 1 | 6 个 HTML 各自内联几百行 CSS，改主题色要改 6 处 | 提取公共 CSS 变量和样式文件 |
| 2 | 没有统一的 auth 模块：每个页面独立读 localStorage | 封装 auth.js，统一处理 Token 过期/登出 |
| 3 | 轮播图链接指向带参数的 product.html，但 product.html 是否正确处理了 URL 参数需验证 | 用浏览器打开验证，这正是之前报的"404问题" |
| 4 | 搜索框只在桌面端显示（`@media` 里 `display:none`），移动端无搜索入口 | 移动端加底部搜索栏或汉堡菜单 |
| 5 | 无骨架屏/Loading 状态：商品列表加载前是空白 | 加 Skeleton 或 shimmer 动画 |

### 3.4 运维待补全

| # | 缺失项 | 建议 |
|---|--------|------|
| 1 | 无 HTTPS | Nginx + Let's Encrypt |
| 2 | 无健康检查端点 | 加 `/health` 返回服务状态 |
| 3 | 无监控告警 | Prometheus + Grafana 或简单脚本监控 |
| 4 | 无数据库备份 | 定时 mysqldump 或阿里云自动快照 |
| 5 | MySQL 不在 Docker Compose 内 | 加 MySQL service，一键部署完整环境 |

---

## 四、与主流电商平台差距（定位分析）

你的项目本质是 **AI 导购 demo**，不是要替代淘宝，所以以下对比的目的是帮你理解边界，面试时能讲清楚"我做了什么、没做什么、为什么"。

| 维度 | 你的项目 | 真实电商 | 差距本质 |
|------|----------|----------|----------|
| 商品量级 | 9 条 demo | 百万级 SKU | 数据不是核心能力，能解释 schema 设计即可 |
| 搜索 | SQL LIKE + 双字切分 | ES + 向量 + 纠错 + 联想 | 可以说"已接入向量检索，生产环境换 ES" |
| 推荐 | 规则（预算/分类） | 协同过滤 + 深度学习 | 可以说"当前是冷启动方案，有用户行为后可升级" |
| 支付 | 模拟改状态 | 支付宝/微信支付 SDK | 可以说"支付网关是接入层，核心是订单状态机" |
| 性能 | 单进程无缓存 | 分布式 + CDN + Redis | 可以说"架构上预留了 Redis 配置，可水平扩展" |

**面试话术建议**：不要说"我的项目比不上淘宝"，而是说"我用最小可行产品验证了完整的电商链路，并在 AI 能力上做了深度集成，这是主流电商正在探索的方向"。

---

## 五、下一步改造优先级

### 第一优先：能跑起来验证
- [ ] 本地启动服务，验证所有改造后的接口是否正常
- [ ] 验证 chat.html 流式输出效果
- [ ] 验证下单事务 + 库存扣减是否正确

### 第二优先：提升面试竞争力
- [ ] 写 3-5 个核心链路的 pytest 测试（登录、下单、Agent 调用）
- [ ] 加 `/health` 健康检查端点
- [ ] 加 loguru 日志系统
- [ ] 清理 utils.py / recommend.py 残留依赖

### 第三优先：完善体验
- [ ] 前端提取公共 CSS
- [ ] 加骨架屏 Loading
- [ ] 验证并修复轮播图链接 404
- [ ] 移动端搜索入口

### 第四优先：生产级补齐
- [ ] Nginx 反向代理 + HTTPS
- [ ] MySQL 写进 docker-compose.yml
- [ ] 数据库备份策略
- [ ] 全局错误处理中间件

---

## 六、文件变更清单（本次改造）

| 文件 | 操作 | 说明 |
|------|------|------|
| `db.py` | 新建 | 全局连接池单例 |
| `agent_tools.py` | 修改 | eval→safe_eval、连接池统一 |
| `api_server.py` | 重写 | 370行→188行，删除所有旧端点 |
| `routes/user.py` | 修改 | get_db→全局池 |
| `routes/product.py` | 修改 | get_db→全局池 |
| `routes/cart.py` | 修改 | get_db→全局池 |
| `routes/order.py` | 修改 | get_db→全局池、事务+乐观锁 |
| `routes/ai.py` | 修改 | get_db→全局池、新增流式端点 |
| `routes/admin.py` | 修改 | get_db→全局池 |
| `services/ai_customer_service.py` | 修改 | 新增 chat_stream 方法 |
| `static/chat.html` | 修改 | 接入流式输出、光标动画 |
| `init.sql` | 修改 | products 表补全字段+索引 |
